// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    // For Firebase JS SDK v7.20.0 and later, measurementId is optional
    config: {
      apiKey: "AIzaSyDlYIg8RQlM9H0chp_h83zjJolZofFJ2Vs",
      authDomain: "hrdocapp.firebaseapp.com",
      projectId: "hrdocapp",
      storageBucket: "hrdocapp.appspot.com",
      messagingSenderId: "64372322909",
      appId: "1:64372322909:web:bf1d589c6edc6931edd3b4",
      measurementId: "G-7M1KCBGQPX"
    }
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
